---
name: qh-video-prompt
description: "QH 的默认影视视频提示词模板。用户在影视、剧情、分镜或 AI 视频语境中说‘写提示词’‘给我提示词’‘改提示词’，或要求‘按我的模板/庄园模板’时使用。将剧情、剧本或已有分镜写成参考素材与用途、一句话概述、场景与空间锚点、镜头时间轴、贯穿约束五个栏目。不用于代码提示词、单张场景图或角色资产图；用户另指定格式时以当次要求为准。"
---

# QH 影视视频提示词

为 AICG 导演 QH 编写可直接复制到视频生成工具的完整中文提示词。默认沿用用户提供的庄园车队示例的组织方式、导演描述密度和空间连续性，不将示例剧情当作固定设定。

## 输出模板

每次创作或完整改写前读取 [assets/prompt-template.md](assets/prompt-template.md)，严格按以下次序输出五个栏目，保留中文方括号标题：

1. 【参考素材与用途】
2. 【一句话概述】
3. 【场景与空间锚点】
4. 【镜头时间轴】
5. 【贯穿约束】

使用连续正文和分镜段落，不默认改成表格、JSON、关键词串或另起一套提示词结构。直接交付成稿，不附带无关分析、评分、方案选项或复述规则。用户只修改某一镜或某个栏目时，遵守其局部修改范围；用户要求完整稿时输出全部五栏。

用户原稿保存在 [references/manor-original.md](references/manor-original.md)。仅在需要对照示例的写法、细节密度，或继续这段庄园剧情时读取；它是原始示例，不是每次必须复用的内容。

## 创作与修订

- 先保留用户给定的剧情顺序、人物关系、原台词、素材名称、数量、镜头意图和明确时长。将简略描述补成可见、可拍的动作与表情，不凭空增加改变情节的事件。
- 已有分镜以忠实整理为主；只有在用户授权重排，或实际连贯性要求微调时才调整。轻微导演细节自行解决，实质性剧情冲突或互不兼容的硬性时长要求才简短询问。
- 未指定时长或镜头数时，按动作与对白需要安排，并在镜头时间轴内明确体现；不把示例的 30 秒、7 镜写死，也不无故套用其他 skill 的 15 秒分段或额外双语格式。
- 信息足够就写；不为模板中的每一项追问。仅文字描述也能成稿。创作缺省值按剧情选择，不把自己的推断描述成看过参考图后的事实。
- 新请求明确指定其他模板、风格、语言或平台约束时，以新请求为准；其他技能的专业知识可以辅助，但不擅自替换这份默认输出结构。

## 五个栏目的写法

### 参考素材与用途

- 按用户实际提供的素材逐项列出：`@素材原名 — 参考对象：……；用途：……。` 名称保留大小写、中文、数字和下划线，不自行改名或臆造已存在的 @素材。
- 用途要具体，例如身份与服装、建筑布局、局部景观、车辆外观、车内座位关系、特定光线方向；明确“用来锁定什么”。同一素材可以承担多个具体用途。
- 人物有音色引用时附 `音色：@音色原名`；仅沿用用户已有的音色绑定，不替用户创建音色来源。有明确数量或状态时一并写清，例如“车辆 × 3；头车车窗打开”。
- 只有文字描述而没有可查看的素材文件时，可保留用户已给出的引用和用途，但不声称已核对视觉内容。完全没有素材时写“无指定参考素材；按下述剧情与视觉设定生成”，不为填满栏目虚构素材。

### 一句话概述

用一句完整的话串起主体、核心事件、主要镜头组织和情绪或叙事目的，让读者立即知道这段视频发生什么、希望观众感受到什么。

### 场景与空间锚点

先写日/夜、内/外、时代与地点，再说明主要区域的前后左右、人物起始位置、道具状态、行动路线和光源方向。只写会影响本段拍摄的锚点。

对多主体场景明确总数与分布，局部描述与总数一致；对门、窗、道路、座位、楼梯等关键结构交代可贯穿各镜的状态。环境左右与画面左右分别说清，需要时用入口、主楼、道路等固定地标定义方位。

### 镜头时间轴

按 `分镜一（0-4秒）：` 编号，依次使用“分镜二、分镜三……”；时码表示成片播放时间，从 0 秒起连续衔接、不留空档、不重叠，并在末镜达到总时长。默认用整数秒，用户指定更精确的时码则保留。

每镜固定按以下顺序写四个字段；有对白才追加台词行：

- `景别：` 明确主体及观察尺度，如高位大远景、双人中近景、人物近景。
- `构图：` 交代机位与朝向、主体在画面的位置、前中后景、视线关系及必要遮挡。区分主体位于画左/画右与其身体左/右。
- `镜头运动：` 写明固定、推、拉、移、摇或跟随，以及方向、速度、幅度和跟随对象。车载等镜头区分摄影机相对车辆与相对地面的运动，避免“固定”和“跟移”互相矛盾。
- `画面内容：` 按先后顺序写动作、表情变化、主体之间的互动、环境响应及本镜关键光影。用可观察的微动作承载情绪，保留接触、承托、重量和惯性。
- `台词：角色（情绪/语气）：“原台词”`。保留原语言、措辞、说话人和先后顺序；用户未要求翻译时不加双语。区分现场对白、画外音和旁白，不凭空增加台词。无对白则省略此行。

先按台词的正常情绪语速与停顿估算说话时间，再分配动作与反应时间；不要用赶读挤进不够的镜长。慢动作标明倍率或播放速度，镜长仍按成片播放时长计算，例如“2 倍升格，以 1/2 速度播放”。不要无意间把正常对白变成拉长的慢速语音。

跨镜延续状态：门打开后保持相应开度、车辆按既定顺序行驶、人物不换座、持物和拥抱的左右手不跳变。对同一动作的多镜覆盖明确“开始—继续—完成”，不要每切一镜都重新发生；有意回放或重演则明确写出。

### 贯穿约束

用一段或数段连贯正文，统一适用于整段的画面质感、色彩、材质、照明与曝光、景深、人物和物件一致性、物理运动及声音。镜内独有的信息留在对应镜头，避免机械重复。

按这次剧情选择约束：例如主光方向、暗部层次、真实皮肤与织物、镜面反射、人物接触、车辆转弯惯性、环境声与对白关系。材质细节、光影与声音要服务叙事。示例中的复古黑帮质感、1950 年代、右侧暖日光、黑柔滤镜、父女、8 名保镖、3 辆车和英文对白均非通用默认。

## 交付前自检

检查五栏顺序与每镜字段、全部时间相加是否正确、对白能否自然说完、素材是否来自用户、数量和方位是否一致，以及动作与光线能否跨镜延续。修正成稿中的问题即可，不把检查清单附在提示词后。

本技能的任务是写提示词。用户仅要求文字时，不自动调用生图、生视频、发布或其他外部执行服务。